---
apiVersion: featuremanagement.cloudbees.io/v1alpha1
kind: flag

flag: enablePersonalizedHero
flagType: boolean
description: VIP members see a personalized hero with their name and activity-specific imagery instead of the generic storefront hero. Create a premium, personalized experience for top-tier members. Increase engagement and emotional connection with the brand. Demonstrate dynamic content personalization via feature flags.
labels:

availableValues:
  - "true"
  - "false"

isPermanent: false
