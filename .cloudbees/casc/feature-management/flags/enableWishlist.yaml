---
apiVersion: featuremanagement.cloudbees.io/v1alpha1
kind: flag

flag: enableWishlist
flagType: boolean
description: Allow users to save gear to wishlist for upcoming trips. Beta test feature before general availability. Increase engagement and return visits for trip planning. Test impact on conversion (wishlist to purchase).
labels:

availableValues:
  - "true"
  - "false"

isPermanent: false
