---
apiVersion: featuremanagement.cloudbees.io/v1alpha1
kind: flag

flag: enablePerkPreview
flagType: boolean
description: Show Summit membership perks strip with tier-aware icons (locks for non-VIP, checkmarks for VIP). Drive membership upgrades by showing what basic users are missing. Reinforce value for Summit members. Visual demo impact when switching personas
labels:

availableValues:
  - "true"
  - "false"

isPermanent: false
